<h1>Listes de mes recettes enregistrées</h1>
<div class="container my-4">
  <div class="row g-3">
    <!-- Recipe Title Card -->
    <% @recipes.each do |recipe| %>
    <%= link_to recipe_path(recipe), class: "recipe-card" do %>
          <div class="col-12 col-md-6 col-lg-4">
            <div class="recipe-title-card">
              <%= recipe.name %>
              <%= recipe.content.first(50) %>
            </div>
          </div>
        <% end %>
    <% end %>
  </div>
</div>
